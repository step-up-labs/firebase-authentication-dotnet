<UserControl
    x:Class="Firebase.Auth.UI.Pages.LinksFooter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Firebase.Auth.UI.Pages"
    xmlns:loc="using:Firebase.Auth.UI.Resources"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel 
        Margin="0 24"
        HorizontalAlignment="Right"
        VerticalAlignment="Center"
        Orientation="Horizontal">

        <TextBlock>
            <Hyperlink x:Name="TosHyperlink">
                <Run Text="{x:Bind loc:AppResources.Instance.FuiTermsOfService}" />
            </Hyperlink>
        </TextBlock>

        <TextBlock Margin="12 0 0 0">
            <Hyperlink x:Name="PpHyperlink">
                <Run Text="{x:Bind loc:AppResources.Instance.FuiPrivacyPolicy}" />
            </Hyperlink>
        </TextBlock>

    </StackPanel>
</UserControl>
